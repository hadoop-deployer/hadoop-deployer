#!/bin/bash
# Author: zhenghh@ucweb.com
DIR=`cd $(dirname $0);pwd`
. $DIR/../PUB.sh;
. $DIR/../install_env.sh;
[ -f install_env.sh ] && . install_env.sh

if [ $# != 1 ]; then
  echo "USAGE: $0 SERVER_NAME";
  echo "eg: $0 kpi11";
  exit 1;
fi

ssh $1
